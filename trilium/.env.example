# TriliumNext connection
# Get an ETAPI token from Options > ETAPI in TriliumNext
TRILIUM_URL=http://localhost:8088
TRILIUM_ETAPI_TOKEN=

# Docker mode only — host path for note data
TRILIUM_DATA_PATH=
